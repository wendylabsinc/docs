`wendy project entitlements remove` can be ran with an entitlement as input, or without.

`wendy project entitlements remove <entitlement>` will immediately update the `wendy.json` to omit the entitlement.

Omitting the entitlement from the invocation shows a list of all active entitlements, and an interactive menu to remove them.